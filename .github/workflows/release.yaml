name: 🚀 SuperSpot Wifi Release

on:
  push:
    branches:
      - main # Automatically trigger on push to main
  workflow_dispatch: # Allow manual trigger from GitHub UI

jobs:
  release:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.2
          extensions: mbstring, xml, ctype, json, bcmath, fileinfo
          coverage: none

      - name: 📦 Install Composer dependencies
        run: composer install --no-dev --prefer-dist --optimize-autoloader

      - name: ⚙️ Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: 📦 Install Node dependencies
        run: npm ci

      - name: 🛠️ Build frontend assets
        run: npm run build

      - name: 📦 Prepare release directory
        run: |
          mkdir -p release/public/build
          mkdir -p release/storage

          cp -r app bootstrap config database public resources routes storage artisan release/
          cp -r public/build release/public/build || true
          cp public/mix-manifest.json release/public/ || true
          cp -r vendor release/vendor
          cp composer.* phpunit.xml README.md .env.example release/ || true

          chmod -R 755 release

      - name: 🗜️ Archive release
        run: |
          cd release
          zip -r ../super-spot-release.zip .

      - name: 🚀 Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release from main - ${{ github.run_number }}
          tag_name: main-${{ github.run_number }}
          files: super-spot-release.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
